package com.example.service;

import com.example.model.Organization;

public interface OrganizationService {
	
	/*
	 * Gets the Organization id based on the org. id
	 */
	public Organization getOrgDetails(int id);

}
